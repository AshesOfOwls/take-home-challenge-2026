# Tickets

Kanban board for the Dutchie take-home. Move tickets between columns as you work. Check off sub-tasks as you complete them; a ticket is "done" when it moves to the Done column.

## Working guidelines

- **Scope:** only touch `src/your-storefront.tsx`, `src/product.tsx`, the `ProductCard` component, and supporting files (API/hooks/styles/tests). Do **not** modify the "Read me first!" page (`src/index.tsx`).
- **Commits:** prefer many small, well-scoped commits — one per ticket (or per sub-task on larger tickets).
- **AI use:** allowed, but understand every line; be ready to defend decisions live.

---

## In Progress

_(move tickets here when you start them — limit yourself to 1–2 at a time)_

---

## Backlog

### Task 2: Product card styling

#### T-09 — Style the card container

- [ ] Style `CardContainer` (spacing, borders, etc.).
- [ ] Add a hover/focus state.

_Acceptance:_ card reads as interactive.

#### T-10 — Edge cases on the card

- [ ] Truncate long product names.
- [ ] Add fallbacks for missing `brandName` and empty `prices`.

_Acceptance:_ no overflow or empty UI fragments.

### Task 3: Product Detail Page

#### T-11 — Add `:id` param to the PDP route

- [ ] In `src/app.tsx`, change the `/product` route to `/product/:id` and keep it pointed at `ProductPage`.
- [ ] Leave the `/` (Read me) and `/your-storefront` routes untouched.

_Acceptance:_ visiting `/product/<id>` mounts `ProductPage` with the id available via `useParams`.

#### T-12 — Link card → PDP

- [ ] Update `<Link to="/product">` in `ProductCard` to `/product/${product.id}`.

_Acceptance:_ clicking a card lands on the matching PDP URL.

#### T-13 — PDP data fetching

- [ ] Add a `getProductById(id)` helper in `src/api/products.ts` — the API only exposes `/get-products`, so this fetches the list and filters by id (returns `Product | undefined`).
- [ ] Add a `useProduct(id)` React Query hook keyed by id.
- [ ] Consume it in `src/product.tsx`, reading the id from `useParams`.

_Acceptance:_ direct URL load (`/product/2`) fetches and renders the correct product; unknown ids resolve to `undefined`.

#### T-14 — Replace the PDP placeholder with a real scaffold

- [ ] In `src/product.tsx`, remove the hardcoded placeholder content.
- [ ] Lay out sections for the product image and details.

_Acceptance:_ scaffold renders without crashing, even before content is wired up.

#### T-15 — PDP core fields

- [ ] Render name and brand — required by spec.
- [ ] Render photo — required by spec.
- [ ] Render price, type, strain.

_Acceptance:_ primary identifiers visible above the fold.

#### T-16 — PDP description + effects

- [ ] Render the product description — required by spec (prefer `description` for plain text; only use `descriptionHtml` if sanitized).
- [ ] Render effects — required by spec.

_Acceptance:_ description and effects render.

#### T-17 — PDP secondary fields + back link

- [ ] Render THC/CBD content.
- [ ] Render flavors, weight, options.
- [ ] Add a "Back to storefront" link pointing to `/your-storefront`.

_Acceptance:_ nothing from the `Product` type is conspicuously missing.

#### T-18 — PDP loading + not-found states

- [ ] Render a loading message while the product is loading.
- [ ] Render a not-found message for unknown ids.

_Acceptance:_ bad ids show a not-found message instead of crashing.

### Tests

#### T-19 — Storefront test: renders cards from query

- [ ] Mock `fetchProducts`.
- [ ] Assert N cards render with the right names.

_Acceptance:_ test passes; mirrors `page-wrapper.test.tsx` setup.

#### T-20 — Product card test: fields + link

- [ ] Assert image `alt`, name, brand, and price text.
- [ ] Assert `href` includes the product id.
- [ ] Cover the missing-field fallback.

_Acceptance:_ all assertions green.

#### T-21 — PDP test: renders details + not-found

- [ ] Mock the product query.
- [ ] Assert name, description, and effects render.
- [ ] Cover the not-found path.

_Acceptance:_ happy path + 404 path both green.

### Polish

#### T-22 — Types and tests clean

- [ ] `pnpm exec tsc --noEmit` reports no errors.
- [ ] `pnpm test` is green.
- [ ] Editor shows no TypeScript or unused-import warnings in changed files.

_Acceptance:_ no type errors; all tests pass. (There is no `pnpm lint` script — rely on `tsc` and editor diagnostics.)

### Manual styling

#### T-23 — Manual styling pass

Catalog of visual styling owned by the candidate, not the AI. Scaffolding (empty styled stubs, layout primitives, breakpoints, placeholder values) is in place; the work here is typography, color, spacing, and visual polish.

- [ ] `Brand`, `Name`, `Price` in `src/components/product-card/product-card.styles.tsx` — typography, alignment.
- [ ] `Image` in `src/components/product-card/product-card.styles.tsx` — aspect-ratio, object-fit if needed.
- [ ] `CardContainer` in `src/components/product-card/product-card.styles.tsx` — refine dimensions, border, padding, hover/focus visual treatment.
- [ ] `ProductGrid` in `src/your-storefront.tsx` — refine gap, column widths, breakpoint thresholds.
- [ ] Loading and error `<div>`s in `src/your-storefront.tsx` — typography, spacing, placement.
- [ ] PDP layout and typography in `src/product.tsx` — once T-14 scaffold lands.

_Acceptance:_ every visual decision in the app is the candidate's, not the AI's.

---

## Done

#### T-01 — Create the products API module

- [x] Create `src/api/products.ts` with a `fetchProducts` helper that hits `get-products`.
- [x] Type the response against `Product` and surface a clear error on non-2xx.
- [x] Use it from `src/your-storefront.tsx`.

_Acceptance:_ storefront fetches products via the new module.

#### T-02 — Add a `useProducts` query hook

- [x] Create `src/hooks/use-products.ts` that calls `useQuery` with `fetchProducts`.
- [x] Use the hook from `src/your-storefront.tsx`.

_Acceptance:_ storefront reads products via `useProducts`; no `useQuery` calls live in components.

#### T-03 — Render dynamic product values

- [x] Confirm every field in `ProductCard` comes from the `product` prop — no hardcoded values.
- [x] Spot-check the rendered storefront against the API payload (one card per product, correct data).

_Acceptance:_ every product in the payload appears exactly once with its own data.

#### T-04 — Loading + error states

- [x] Render a loading message while the query is pending.
- [x] Render an error message when the query fails.
- [x] Add `role="status"` to loading and `role="alert"` to error.

_Acceptance:_ both states render and are accessible.

#### T-05 — Storefront grid layout

- [x] Lay out the product cards in a responsive grid in `your-storefront.tsx`.
- [x] Use breakpoints from `utils/media-queries.ts`.

_Acceptance:_ cards reflow across breakpoints.

#### T-06 — Product card scaffold

- [x] Extend `src/components/product-card/product-card.styles.tsx` with styled stubs for brand, name, and price.
- [x] Wire the new styled components into `ProductCard`.

_Acceptance:_ component structure mirrors the mockup hierarchy.

#### T-07 — Style the card image

- [x] Style the `Image` styled component to fit the card layout.

_Acceptance:_ image renders consistently across cards.

#### T-08 — Style card typography

- [x] Style the brand, name, and price text.

_Acceptance:_ visual hierarchy is readable. (Visual typography deferred to T-23; scaffold from T-06 satisfies the structural hierarchy.)
